## Add Bash Alias on Mac
I just open zshrc with sublime, and edit it.

```bash
subl .zshrc
```

And add this on sublime:

```bash
alias blah="/usr/bin/blah"
```

Run this command in terminal:

```bash
source ~/.zshrc
```

Done.